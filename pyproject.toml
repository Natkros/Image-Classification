[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "intel-image-classifier"
version = "0.1.0"
description = "Transfer-learning image classifier for the Intel Image Classification dataset"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
